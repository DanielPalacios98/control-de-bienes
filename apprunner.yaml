version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - bash -c "cd backend && npm ci"
    build:
      - bash -c "cd backend && npm run build"
run:
  command: 'bash -c "cd backend && npm run start:prod"'
